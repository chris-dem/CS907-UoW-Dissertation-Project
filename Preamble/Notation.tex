\newglossaryentry{n_list}{
    name={\ensuremath{[n]}},
    description={List of numbers ${1,..., n}$},
    type=symbols
}


\newglossaryentry{natz_nums}{
    name={\ensuremath{\mathbb{N}_0}},
    description={Natural numbers including $0$},
    type=symbols
}

\newglossaryentry{nz_list}{
    name={\ensuremath{[n]_0}},
    description={Defined as $[n] \cup \{0\}$},
    type=symbols
}

\newglossaryentry{nat_nums}{
    name={\ensuremath{\mathbb{N}}},
    description={Set of natural numbers excluding $0$},
    type=symbols
}

\newglossaryentry{poly-sized}{
    name={\ensuremath{n^{O(1)}}},
    description={Set of all functions $f: \mathbb{N} \to \mathbb{N}$, where $f \in O(n^c)$ for some $c \in \mathbb{N}$},
    type=symbols
}

\newglossaryentry{not-replace}{
    name={\ensuremath{x_{-i}, x_{-i}(a)}},
    description={Given $x \in A^d$, we use $x_{-i}$ to denote all members excluding index $i$. To show that we replace the index $i$ with $y$, we say $x_{-i}(a)$},
    type=symbols
}


\newglossaryentry{kleene-set}{
    name={\ensuremath{\mathbb{T}}},
    description={Defines the set $\{0,1, \bot\}$ which depicts the kleene algebra.},
    type=symbols
}



%TODO
% \newglossaryentry{non-det}{
%     name={Non-Deterministic Turing Machines}
%     description={}
%     type={acronym}
% }